# Core MCP framework (required by all three servers)
mcp[cli]
python-dotenv
pydantic
psutil

# --- Hardware Server ---
numpy
opencv-python

# --- RAG Server ---
sentence-transformers
chromadb

# --- Testing ---
pytest

# --- Scratchpad Server ---
# (No extra Python packages — the sandbox runs inside Docker)
# Docker must be installed separately: https://docs.docker.com/get-docker/

# --- Ingestion Pipeline ---
marker-pdf>=1.3          # Primary PDF-to-markdown (layout model, LaTeX/heading support)
pymupdf4llm>=0.0.17      # Fallback PDF-to-markdown (font-size heuristics)
pdfminer.six>=20221105   # PDF metadata extraction from document info dict
watchdog>=4.0            # Filesystem event monitoring for the drop/ folder
httpx>=0.27              # Optional arXiv API metadata enrichment

# --- Optional: real hardware drivers ---
# Uncomment if using a real ImagingSource GigE camera:
harvesters
#
# The Thorlabs PDXC SDK (PDXC_COMMAND_LIB) is bundled in devices/vendor/
# and does not require a separate pip install.
